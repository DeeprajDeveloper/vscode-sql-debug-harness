{
  "name": "sql-debug-harness",
  "displayName": "SQL Debug Harness",
  "description": "Generate safe T-SQL debug harness scripts and analyze stored procedures from VS Code — no Python required.",
  "repository": {
    "type": "git",
    "url": "https://github.com/DeeprajDeveloper/vscode-sql-debug-harness"
  },
  "license": "MIT",
  "version": "0.0.1",
  "publisher": "DeeprajAdhikary",
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=18"
  },
  "icon": "images/extension-logo.png",
  "categories": [
    "Other",
    "Programming Languages"
    ],
  "keywords": [
    "sql",
    "tsql",
    "sql-server",
    "stored-procedure",
    "debug-harness"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "bin": {
    "sql-debug-harness": "./out/cli.js"
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "sql-debug-harness",
          "title": "SQL Debug Harness",
          "icon": "images/activitybar-icon.svg"
        }
      ]
    },
    "views": {
      "sql-debug-harness": [
        {
          "id": "sqlSpHarness.sidebar",
          "name": "Workbench",
          "icon": "$(window)",
          "contextualTitle": "SQL Debug Harness"
        }
      ]
    },
    "viewsWelcome": [
      {
        "view": "sqlSpHarness.sidebar",
        "contents": "Open the SQL Debug Harness workbench to analyze procedures and generate safe debug scripts.\n\n[Open Workbench](command:spDebug.openWorkbench)\n\n[Analyze Active SQL](command:spDebug.analyze)\n\n[Generate Debug Script](command:spDebug.generate)\n\n**Tip:** In the workbench, use **Select File…** to pick a workspace `.sql` file, or **Load Active** for the open editor."
      }
    ],
    "commands": [
      {
        "command": "spDebug.generate",
        "title": "Generate Debug Script",
        "category": "SQL Debug Harness",
        "icon": "$(debug-alt)"
      },
      {
        "command": "spDebug.analyze",
        "title": "Analyze Procedure",
        "category": "SQL Debug Harness",
        "icon": "$(list-flat)"
      },
      {
        "command": "spDebug.openWorkbench",
        "title": "Open Workbench",
        "category": "SQL Debug Harness",
        "icon": "$(window)"
      },
      {
        "command": "spDebug.openInWorkbench",
        "title": "Open in Workbench",
        "category": "SQL Debug Harness",
        "icon": "$(window)"
      },
      {
        "command": "spDebug.configure",
        "title": "Configure Settings",
        "category": "SQL Debug Harness",
        "icon": "$(settings-gear)"
      },
      {
        "command": "spDebug.openSettings",
        "title": "Open Extension Settings",
        "category": "SQL Debug Harness",
        "icon": "$(settings)"
      }
    ],
    "submenus": [
      {
        "id": "spDebug.submenu",
        "label": "SQL Debug Harness",
        "icon": "$(debug-alt)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "spDebug.openWorkbench",
          "when": "view == sqlSpHarness.sidebar",
          "group": "navigation@1"
        },
        {
          "command": "spDebug.analyze",
          "when": "view == sqlSpHarness.sidebar",
          "group": "navigation@2"
        },
        {
          "command": "spDebug.generate",
          "when": "view == sqlSpHarness.sidebar",
          "group": "navigation@3"
        }
      ],
      "spDebug.submenu": [
        {
          "command": "spDebug.openWorkbench",
          "group": "1_run@0"
        },
        {
          "command": "spDebug.generate",
          "group": "1_run@1"
        },
        {
          "command": "spDebug.analyze",
          "group": "1_run@2"
        },
        {
          "command": "spDebug.configure",
          "group": "2_setup@1"
        },
        {
          "command": "spDebug.openSettings",
          "group": "2_setup@2"
        }
      ],
      "commandPalette": [
        {
          "command": "spDebug.openWorkbench"
        }
      ],
      "explorer/context": [
        {
          "command": "spDebug.openInWorkbench",
          "when": "resourceExtname == .sql",
          "group": "navigation@9"
        },
        {
          "submenu": "spDebug.submenu",
          "when": "resourceExtname == .sql",
          "group": "7_modification"
        }
      ],
      "editor/context": [
        {
          "command": "spDebug.openInWorkbench",
          "when": "resourceLangId == sql",
          "group": "navigation@9"
        },
        {
          "submenu": "spDebug.submenu",
          "when": "resourceLangId == sql",
          "group": "navigation"
        }
      ],
      "editor/title/context": [
        {
          "submenu": "spDebug.submenu",
          "when": "resourceLangId == sql",
          "group": "navigation"
        }
      ]
    },
    "configuration": [
      {
        "title": "SQL Debug Harness › Generate",
        "order": 0,
        "properties": {
          "spDebug.traceStyle": {
            "type": "string",
            "enum": [
              "raiserror",
              "print"
            ],
            "enumDescriptions": [
              "RAISERROR … WITH NOWAIT (Messages tab in SSMS)",
              "PRINT traces (default)"
            ],
            "default": "print",
            "order": 1,
            "description": "Trace line style for generated debug scripts."
          }
        }
      },
      {
        "title": "SQL Debug Harness › Logging",
        "order": 1,
        "properties": {
          "spDebug.logToOutput": {
            "type": "boolean",
            "default": true,
            "order": 1,
            "markdownDescription": "Write the step-by-step transform/analyze log to the **SQL Debug Harness** output channel."
          },
          "spDebug.saveLogFile": {
            "type": "boolean",
            "default": false,
            "order": 2,
            "description": "Also save the step log as <procedure>.log beside the source .sql file (when saved on disk)."
          },
          "spDebug.quietWhenLogging": {
            "type": "boolean",
            "default": true,
            "order": 3,
            "description": "When step logging is enabled, avoid duplicating progress lines in the Output channel."
          }
        }
      }
    ]
  },
  "scripts": {
    "compile": "node esbuild.js",
    "check": "tsc -p ./ --noEmit",
    "watch": "node esbuild.js --watch",
    "test": "jest",
    "vscode:prepublish": "npm run compile",
    "package": "npm run compile && mkdir -p dist && vsce package --out dist/sql-debug-harness.vsix --allow-missing-repository",
    "docs:build": "bash scripts/build-docs.sh",
    "docs:pdf": "bash scripts/build-docs.sh --pdf",
    "docs:serve": "npm run docs:build && npx --yes serve docs -p 8080"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.19.43",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "2.32.0",
    "esbuild": "^0.28.1",
    "jest": "^30.4.2",
    "md-to-pdf": "^5.2.4",
    "sass": "^1.77.0",
    "ts-jest": "^29.4.11",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "node-sql-parser": "^5.4.0"
  }
}
