CREATE PROCEDURE dbo.usp_WithTcl
    @Id INT
AS
BEGIN
    BEGIN TRANSACTION
    UPDATE dbo.T SET V = 1 WHERE Id = @Id;
    COMMIT TRANSACTION
    BEGIN TRAN
    DELETE FROM dbo.T WHERE Id = @Id;
    ROLLBACK TRANSACTION
    SAVE TRAN savepoint1
END
